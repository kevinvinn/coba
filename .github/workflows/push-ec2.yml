name: Push-to-EC2

# Trigger deployment only on push to main branch
on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to EC2 on main branch push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the files
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: Install Dependencies
        run: npm install

      # Build your project (assuming you're using something like Webpack, or any other build tool)
      - name: Build the project
        run: npm run build # Sesuaikan dengan script build proyek Anda

      - name: Deploy to Server 1
        run: |
          sudo rsync -avz --delete ./dist/ ${USERNAME}@${HOST_DNS}:/home/ubuntu/coba
        env:
          USERNAME: ${{ secrets.USERNAME }}
          HOST_DNS: ${{ secrets.HOST_DNS }}
          TARGET_DIR: ${{ secrets.TARGET_DIR }}
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
